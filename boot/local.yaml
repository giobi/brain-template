# boot/local.yaml — Where this brain runs
#
# Describes the machine and available capabilities.
# Update when infrastructure changes.

# === Machine identity ===
platform: local               # server | local | container
hostname: ""
os: ""
arch: ""

# === Capabilities ===
# Uncomment and fill in what you have available:

# services:
#   git: true
#   python3: true
#   node: true
#   docker: true

# capabilities:
#   web_serving:
#     url: https://...
#     root: public/
#   email:
#     provider: gmail
#     address: ...

# === Secrets ===
secrets:
  path: .env
  format: dotenv
